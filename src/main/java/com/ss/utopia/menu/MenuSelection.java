package com.ss.utopia.menu;

/**
 * A marker interface the represents a menu selection. A menu selection
 * may be a sub menu, in which case, it can be run like a regular menu,
 * or it can be a menu operation, where it will run a final operation
 */
public interface MenuSelection {
}
